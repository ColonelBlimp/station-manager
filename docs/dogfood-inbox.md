# Dogfood inbox

Raw dogfooding notes for later triage.

## Notes
- [2026-09-06] another item I noticed: when starting smd, if it fails to start for some reason, the user is not informed of this. The first they get know about it is that the SPA does not load. Is there something we can do to make this failure more immediately visible?
- [2026-09-10] implement export as Excel spreadsheet, like ADIF export
- [2026-09-11] the mode selector is poluted and does not make for easy reading - simplify — routed 2026-09-12 to W-0012 (Rig Control mode control in the FT views; the Phone/CW list tidy awaits rulings there); build after the contest — built 2026-09-13 (FT views: Mode is a readout owned by the profile), awaiting deploy
- [2026-09-11] FT4 live run: with a second SPA tab open (Map), the map reported "cannot reach the daemon" and Band Activity looked unable to keep up with 7.5 s slots; the daemon log showed its SSE subscriber count (global across event endpoints) reach 6 and stream connections lasting 11–27 s. The two tabs together held six streams: the shell's log and rig streams in each, the FT8 stream in Operate, and the Map view's own second log stream. Inference from this incident (not a cited limit): six long-lived streams fill a ~6-per-host HTTP/1.1 connection cap in the browser, so ordinary requests (map fetch, enrichment, contest-dupe, count) queue behind them; closing the extra tabs restored the map at once (measured). Follow-ups: (a) the map should reuse the shell's always-on log stream (ADR 0079) instead of opening its own; (b) cap enrichment lookup concurrency in the SPA (23 first-sight lookups in one minute at 1.5–2.5 s each) so operator actions never queue behind them; (c) longer term HTTP/2 over TLS, which would lift such a cap (inference, to verify); (d) document "one tab" until then. — Recurred 2026-09-13 16:23 local on the new build with the map in a second tab: the daemon's subscriber count hit 6 the instant the Map tab opened (its shell `/v1/events` + `/v1/rig/events` + the Map view's own second `/v1/events`, beside the Operate tab's three) and the map's data request never reached the daemon at all — no `/v1/logbook/{id}/qso` line in the log, only the JS asset — so the browser queued it behind six streams until the SPA gave up. The enrichment cap (b) does not touch this: it is the STREAM count. Proposed, SPA-only: (a) `openLogEvents` multiplexes one EventSource per tab with a handler registry so the Map view rides the shell's stream (−1 per Map tab); (e) open `/v1/rig/events` only in views that need it (the Map tab does not — ruling: its header rig chip would read unknown there). (a) alone leaves one free slot; (a)+(e) leave two. — Ruled 2026-09-13 on (b): cap at 2 concurrent /v1/enrich/callsign requests per SPA tab; worked-before checks stay outside that budget; keep the documented current-slot/answerer priority and stale-work cancellation. W-0012. — (a) built 2026-09-14 (W-0012, ADR 0079 dated update): one `/v1/events` connection per tab, the map joins the shell's; a Map tab now holds two streams, not three. Awaiting deploy. (e) still awaits its ruling. Design exchange the same day, operator: "If this becomes an issue again, we could move the map to the Dashboard, as that real estate is currently unused" — and what was planned for the Dashboard. Answer: ADR 0044 endorsed the Dashboard in principle as a lean status home (rig connection state, forwarding-queue depth/health, today's QSO count, quick-nav cards) composed from existing endpoints and the events hub, no `GET /v1/dashboard`; it is unbuilt (App.svelte renders a dashed placeholder) and nothing is scheduled for it — the whole-log map is a W-0014 deferred item and ADR 0049 kept the time-window map as its own tab for the second-monitor case. Moving the map into the Dashboard would drop the second tab and with it its two streams entirely (a Dashboard map in the Operate tab rides the shell's stream already), but it trades away the second-monitor use. Not selected; revisit if the stream count bites again after (a) is on air, or when the Dashboard is built.
- [2026-09-11] FT4 live run: transient TX ALARM ("rig reports CAT TX still keyed after unkey") twice in ~50 rungs (19:41:28, 19:53:43 local), each cleared within the same second after the bridge re-sent tx_off and the rig reported idle; the post-TX ft8 mode restore was skipped for those rungs, the next rung keyed normally. Looks like the post-unkey confirmation runs a beat before the rig's TX status catches up on the 7.5 s cadence — worth a look at the confirmation window / poll timing before treating it as a real alarm. Not tied to repeats: one fired on a first-attempt rung, the other on the 6th (max) repeat to a JA station; in both the rig's tx-status 2→0 reports landed just AFTER the post-unkey check instead of before it as on every other rung. — Routed 2026-09-13 to W-0011 (SELECTED, backlog P1 #2 by operator ruling). Built 2026-09-13: the first still-keyed answer now re-sends the stop and asks again before any alarm; ms log timestamps.
- [2026-09-12] Settings → Forwarding → QRZ showed "1 queued · 0 in flight": the one row is a terminal `failed` qrz insert from 2026-08-06 06:33Z (QSO 7025, 7Q7EB 20 m SSB) whose `last_error` stores QRZ's response verbatim — "QRZ authentication rejected: invalid api key <16 hex chars>". The token has the shape of a QRZ logbook API key (16 hex characters, dashes stripped); it does NOT match anything in the current config.json (key since rotated), but the pattern stands: upstream error text is stored as-is in `qso_upload.last_error` and from there reaches `smd.log` (0644) and `GET /v1/qso/{uuid}/uploads`. Check `qrz.classifyResponse` — redact or drop the echoed key before storing the message (diagnostics-can-leak-secrets, ST-4 family). Not urgent: the leaked value is stale. — Fixed 2026-09-13 (`internal/forwarding/qrz/qrz.go` `redactKey`): the configured key and its dash-stripped echo are replaced by `[REDACTED]`, case-insensitively, in the AUTH reason and in non-2xx body snippets before classification; the stale row keeps its old text. Awaiting deploy.
- [2026-09-12] Phone/CW: typed a callsign as a UI test, did not clear it, flipped to FT8 and later back to Phone/CW — the fields were still populated. By design today, not a defect: the draft and its QSO clock are module state in `qso.svelte.ts` (`draft`, `qsoClock`), and nothing on a route or mode change clears them — only Esc, Shift+Enter (stack), the Clear button or a successful log do — so a peek at FT8 mid-QSO cannot lose a contact in progress. Ruling wanted: keep as is, clear on a mode switch, or keep but surface the age of a committed draft (the nearest confusable outcome is a draft committed an hour earlier logged with its stale Time On). Routed nowhere yet; W-0012 if selected. — Ruled 2026-09-13: keep the draft across mode switches and show its age; preserve the original Time On, never silently clear or retimestamp; show the age whenever the draft survives a switch, no expiry threshold. W-0012.
- [2026-09-12] when the Band Activity filter is enabled, there should be some inidcator marking it as suc, otherwise the user can sit wondering why there are no decodes in the Band Activity when they have been filtered: i.e. ZS and there are no ZS station on freq — Built 2026-09-13 (`Ft8BandActivity.svelte`): a header chip "Filter: ZS · N hidden" with its own clear, and an empty state that names the filter and the hidden count instead of the quiet-band text; `docs/ft8.md` updated. Awaiting deploy.
- [2026-09-12] FT4 the Channels selection is different to FT8: 2720Hz, 2804Hz, 452Hz, etc — triaged 2026-09-12: working as designed, not a defect. A Channels cell is exactly one signal wide and the grid starts at the passband floor (`Ft8OccupancyStrip.svelte`: `passbandLow + i × signalWidth`); the width comes from the active profile (`internal/ft8/profile.go`: FT8 50 Hz, FT4 84 Hz = ceil(4 × 20.833 Hz)), so FT4 cells fall on 200 + 84k (452 = k 3, 2720 = k 30, 2804 = k 31) and there are 33 of them instead of 56. Only `docs/ft8.md` still describes the strip in FT8-only terms (~50 Hz cells, ≈56 across 200–3000; lines ~535, ~562, ~999, ~1148) — wording fixed 2026-09-12 in `docs/ft8.md` (strip, Spectrum, detection tiers, structural constants; the dated step (e) record gained a pointer), uncommitted.
- [2026-09-12] FT4 Band Activity shows a station calling CQ on 874Hz but nothing is seen in the Spectrum display — triaged 2026-09-12: working as designed, but a confusable worth a cue. The Occupancy panel (both Channels and Spectrum) shows the snapshot of the slot YOU TRANSMIT in — idle, the Even/Odd "TX slot" toggle (default Even); in a QSO, the opposite of the worked station — never a waterfall of the whole band. The 874 Hz station was A61DD (ft8-all.txt 14:12:52Z–14:22:22Z, every decode at :07.5/:22.5/:37.5/:52.5 = odd slots), so it can only ever appear in the Odd snapshot; with the panel on Even (the slot you would answer it in) its absence is correct — the panel is telling you what else is on 874 Hz in YOUR slot. Decode-derived bands are in the report (`occupancy.go` decodeBands, never gated), so a same-parity decode does render. Routed 2026-09-12 to W-0012 ("Show the picked station's own offset on the Occupancy panel", not selected): ghost the picked row's offset on both views as "their signal · other slot".
- [2026-09-12] contest log watch: the transient TX ALARM recurred once — 17:34:20 local (15:34:20Z), the "CQ AF 7Q5MLV KH78" rung at 2822 Hz (repeat 11, tx_gen 261, keyed 5461 ms, meters normal): "rig reports CAT TX still keyed after unkey" + "skipping ft8 mode restore — unkey unconfirmed" at :20, tx_off re-sent (attempt 1) at :21, the rig's tx-status 2 then 0 at :21 and "tx alarm cleared (transmitter confirmed idle)" in the same second; the next rung keyed normally at :30. Same shape as the 2026-09-11 pair: the rig's 2→0 reports landed just after the post-unkey check. Four occurrences over the two FT4 days (the fourth at 18:00:05 local, a "calling" rung answering ZS6KBS, repeat 2, cleared at :06 the same way); every one self-cleared within a second; two on CQ rungs, one the 6th repeat to a JA station mid-exchange (2026-09-11), one answering a station. Rate today: two in the first contest hour (~240 rungs). Adds to the confirmation-window / poll-timing item above. — Prioritised 2026-09-13 (operator ruling): first item after the contest close-out, ahead of every other post-contest item — a false alarm makes the operator doubt the rig or the software; W-0011. Built 2026-09-13 (see the 2026-09-11 note), awaiting deploy.
- [2026-09-13] contest review (record entry 37): the Call-CQ run worked ZS6BOS twice on 20 m and twice on 40 m during the contest — full exchanges an hour apart, answering our CQ each time. The SPA's worked-before flag (`/v1/contest-dupe`, 153 lookups during the run) is informational; the daemon-side sequencer never consults it and cannot read the logbook under the FT8 isolation rule. Ruling wanted (W-0019 open rulings): keep answering, skip flagged callers under a contest flag, or surface the repeat in the ladder. — Ruled 2026-09-13: surface in the ladder, hold before TX, mark as already worked, operator chooses Answer anyway or Next; a failed/unknown lookup never blocks. W-0019.
- [2026-09-13] contest review: "ft8 seq: rung deferred — decode landed too late" fired 17 times, fire request 2.35–6.61 s into our slot (five between 2.35 and 2.57 s, twelve later), but the line does not say whether the request was an operator pick or a decode arriving late — the two mean different things for AC7 (decode latency) and the line should carry the source. Logging gap, not a defect; fold into the AC7 debug window.
- [2026-09-13] FT8 view, Rig Control Mode readout (W-0012, deployed 2.0.0-alpha.2-82) during a Tune: the readout showed "RTTY-U — not the FT8 data mode (DATA-U) — pick the band to set it" on an amber border while the daemon's own tune carrier was keyed (13:24:49–13:25:45 local, four tunes, 20 W RTTY-U, each restored on stop). The rig really reports RTTY-U then — the tune controller switches to it and restores DATA-U afterwards — so the readout is literally right but the advice is wrong for that moment: picking a band mid-tune would write set_freq + set_mode under a keyed carrier. A confusable the slice did not enumerate (its AC2 lists a hand change on the rig, not the daemon's tune). Fix: while `rig.tuneActive`, read "RTTY-U · tune carrier — DATA-U returns when it stops", neutral border, no band-pick hint. — Built 2026-09-13 (W-0012 AC2b), awaiting deploy.
- [2026-09-13] "Nothing listed in the Notifications section" during the FT8 run — working as designed, but an expectation gap: the rail is the DURABLE notification history (W-0001 / ADR 0076) and records only failures the operator must not lose — `export.adif_failed` and terminal `forward.failed` — never activity (QSOs logged, alarms, disarms, session ends live in toasts and `smd.log`). `operator_event` holds 0 rows: no export or upload has failed since the feature shipped (the one terminal QRZ failure, 2026-08-06, predates it). Ruling wanted: keep the rail failures-only, or add an operator activity feed (candidates: TX alarms and their clears, repeat holds, disarms, session ends, dial-guard stops) — a design change, W-0012 if selected. Follow-up the same session: the operator sees this as a log viewer filtered by priority and asked whether the log is DB-backed — it is not (`smd.log`, lumberjack, 100 MB × 5 backups, 30 days; 49 MB today). ADR 0061 already records the shape (all logging categorised into one table; measured 99.5 % info, so an event store, not a mirror) and ADR 0076 shipped its first category (`notification`); the `daemon` category (warn/error/fatal records written from the logging health writer into `operator_event`, rail gains category + severity filters) is the designed-but-unbuilt answer. Rulings wanted: severities captured (recommend warn + error, never info), table retention, rail vs own view.
- [2026-09-13] repeat hold, first on air (FT8 17 m, build alpha.2-82): SQ3F held 16:15:00 local (already worked on 17 m FT8), SP6FCQ held 16:17:00 with SQ3F skipped as `repeat_while_another_held`, SQ3F held again 16:18:00 and 16:21:00. No Answer anyway / Next was recorded: each hold ended when the run committed another contact, and the repeat calling again opened a fresh hold — by design (a commit-cleared hold is not a decision), but a persistent repeat re-announces itself on every cycle; a refinement to consider is remembering a station held once until the operator acts on it or the run ends. The operator saw the box (2026-09-13). Design exchange the same session: doing nothing never answers the held station (a hold clears on another commit, on 3 min of silence, or with the run) — so "Next" looks redundant; it is not: it is the only control that REMEMBERS the station for the rest of the run and stops the re-announcement. Options recorded: keep it under a label that says what it does ("Skip this run" / "Ignore" — recommended), or drop it and make a held station remembered until answered or the run ends (then the box needs a close). Ruled 2026-09-13: keep it, labelled "Ignore" — built.
- [2026-09-13] operator saw the "Repeat held" box during the 17 m run, but it overflows: the box sits inside the Operate panel's fixed-height enrichment zone (h-45, kept fixed so the slot pill and ladder never reflow), so its bottom drops below the rule between that zone and the ladder. Fix: render the hold as its own full-width block between the rule and the slot pill (the ladder shifts down only while a hold stands — an occasional state, not the per-slot jitter the fixed height guards against). — Built 2026-09-13, awaiting deploy.
- [2026-09-13] "In the enrichment section, we can remove the text 'Your next contact…' it's another waste of space" — the run surface's idle auto-mode state line (ADR 0067 ratified wording). Built 2026-09-13 (W-0012): no text in that state, row height held. Deployed 2026-09-13. Follow-up 2026-09-14: "there is a bullet in the enrichment section which needs to be removed" — the state dot left alone beside the empty line; built (dot only beside a state line). Awaiting deploy.
- [2026-09-13] "The 'Calling CQ' can also be removed from the same section, as this text is repeated in the header bar" — the role label under the worked call duplicated the header bar's. Built 2026-09-13 (W-0012): header bar only. Deployed 2026-09-13.
