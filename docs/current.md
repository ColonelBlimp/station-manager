# Current work

Updated: 2026-08-31

- **Goal:** W-0008 (harden audited contract boundaries). Slices 1–2 are complete and pushed. Slice 3's alpha.2 sequence AW-2→AW-3→AW-5→AW-6→AW-4→AW-1 is complete; AW-1 remains open only for its release-gated alpha.3 removal. Slice 4 (frontend-wire) finding F-03 is complete and pushed. [`docs/backlog.md`](backlog.md) owns priority.
- **State:** Slice 4 F-03 is shipped: the SPA validates wire records at its success and safety-state boundaries — the QSO submit success contract (F-03a, `ef6d9141`), per-event RF/FT8 SSE frames (F-03b, `d43720ce`), and list/page/edit records (F-03c, `eb23d1cf`); a malformed frame is dropped, leaving the last-known-good state. [`ADR 0077`](decisions/0077-spa-runtime-wire-decoders.md) records the design. AW-1 alpha.2 remains complete: `qso.*`/`forward.*` events carry `qso_uuid`; public QSO responses use an explicit projection; SM Cloud payloads omit daemon-local QSO/logbook ids; the SPA keys selection, rendering, edit replacement, and re-enrichment on UUIDs; deprecated public numeric QSO identities remain for alpha.2 compatibility only (ADR 0016; [`W-0008`](work/W-0008-harden-audited-contract-boundaries.md) owns the finalized alpha.3 checklist). All the above is pushed (origin/main = `eb23d1cf`) and CI-green.
- **Next:** F-04 (the ambiguous-write outcome-unknown policy — Slice 4's sibling finding) is the likely next item, pending operator selection. Do not pull the alpha.3 removals into alpha.2; execute that checklist as one coherent `v2.0.0-alpha.3` change. [`W-0017`](work/W-0017-deflake-bridge-sse-streaming-test.md) is latent; W-0002 remains RF-gated.
- **Decisions not to revisit:** W-0004 named palettes DECLINED. PT-6 `fsOps` stays package-private — no cross-package test seam.
- **Do not:** re-open a closed dossier (W-0001/W-0003/W-0004/W-0005); initiate RF/hardware without per-occasion agreement; amend or push without operator direction.
- **Relevant files:** [`backlog`](backlog.md), [`W-0008`](work/W-0008-harden-audited-contract-boundaries.md), [`ADR 0016`](decisions/0016-sm-cloud-deferred-with-prep.md), [`ADR 0077`](decisions/0077-spa-runtime-wire-decoders.md), [`API reference`](v2-design/api-endpoints.md).
- **Coordination:** Leave committing and pushing to the operator; non-Markdown commits draw a codex review to triage.
