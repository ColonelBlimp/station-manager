# Station Manager — project instructions for Claude Code

## Repository structure

- **`main`** is the v2 codebase. v2 is a clean-slate rewrite around a daemon + clients + bridges model (decided 2026-04-14). The structure.md "Migration from main's current state to milestone 1" restructure already ran (session-8 cluster): Wails `apps/` directory removed, `go.work` removed, single `go.mod` at the repo root, daemon (`cmd/smd`) and its new internal packages exist (`internal/api`, `internal/qsoservice`, `internal/events`, `internal/safego`), `internal/forwarding/` reshaped from v1's hardcoded-QRZ into the multi-destination `Forwarder` interface + worker + registry. Milestones 1, 1b, 1c are shipped (audited 2026-05-02).
- **UI toolkit progression on main:** Wails (planned, never built in v2) → Gio (decided 2026-04-21, spike landed, parked) → browser SPA (Svelte 5, decided 2026-04-30 per ADR 0001, scaffold shipped same day, under active development at `frontend/logging/`, embedded in the daemon via `//go:embed` and served at `GET /` when `Protocol=tcp && ServeSPA=true`).
- **`internal/bridge` subsystem** per ADR 0013 + ADR 0019 — daemon subsystem providing `/v1/rig/events` SSE for the logging SPA. v1 is **read-only**: rig pushes state via AUTO-mode CAT, bridge filters and forwards via SSE, SPA displays. **M3a (bridge subsystem v1) closed 2026-05-11** — all four sub-milestones shipped (M3a.1 skeleton + SSE plumbing, M3a.2 real serial+CAT pipeline, M3a.3 bootstrap-on-SSE-open via rigdef-driven `READ` + `bridge-error` events with hub cache for late subscribers + identity verification, M3a.4 SPA `bridge.svelte.ts` EventSource consumer + live rig test on the FTdx10). **Rig-mode → ADIF translation also SHIPPED session 51 (2026-05-11):** bridge stays a pure pass-through (rig literal on the wire); SPA resolves to ADIF (MODE, SUBMODE) pairs via per-rig mappings stored in two layers — rigdef-shipped defaults in `internal/cat/rigs/*.json` under `mode_mappings` + operator overrides in `config.json` under `bridge.mode_mappings`, merged at `/v1/config` GET time. New My Station → Mode Mappings sub-tab edits the override layer. **Daemon error codes + SPA i18n SHIPPED session 51 continuation (2026-05-12; ADR 0010 revision 6):** `bridge-error` and `rig-disconnected` payloads now carry `{code, details}` instead of human strings; SPA's `lib/i18n/` catalogue (`en.ts` baseline, planned `ny.ts`/`tum.ts` for Chichewa/Tumbuka) keys off code + substitutes details into localised templates. Operator retunes wording by editing `en.ts` — single file, no daemon restart. **Side-effect win:** `internal/enums/modes` is now data-driven — embedded `adif-modes.json` baseline + optional `$SM_WORKING_DIR/modes.json` operator override loaded at daemon startup; ADIF spec growth no longer requires a daemon binary release. Daemon-authoritative CAT-enable: SPA hydrates `configState.station.enabled` from `/v1/config` `bridge.enabled`; `/v1/config` also exposes `bridge.rig_name`, `bridge.driver`, `bridge.rig_modes`, `bridge.mode_mappings`. Default deployment is single-binary (daemon imports the bridge package); split-host `cmd/bridge` is opt-in and parked. **Pipeline supervisor SHIPPED 2026-05-16 (ADR 0020):** `runPipeline` is now wrapped by `runSupervisor` which classifies exits (permanent / transient / context-cancelled) and retries transient faults with exponential backoff (1s start, 30s cap; defaults — operator-tunable via `bridge.timeouts.{backoff_initial_ms,backoff_max_ms}`) so first-boot ordering (FTdx10's `/dev/ttyUSBn` appears only when rig is powered on) and mid-session disruption (power spike, USB reseat) self-heal without a daemon restart. INIT (`AI1;`) + READ (`ID;FA;FB;ST;VS;MD0;MD1;PC;`) re-fire each supervisor cycle so AUTO mode is re-armed and a fresh snapshot is pulled; toast dedup via `Service.lastPublishedExitKey` prevents flood across retries. **Liveness timeout default lowered to 5s** (was 30s) since the FTdx10 family doesn't surface kernel disconnects on rig-off — 30s detection latency was longer than typical operator rig-off duration; the no-data probe makes false-positives self-recover. Operator-tunable via `bridge.timeouts.liveness_ms`. SPA-side: `bridge-disconnected` toasts are sticky (`ttl=0`) and explicitly dismissed when the reconnect handler fires; reconnect emits `bridge.reconnected` info toast for symmetric closure. **Flash suppression (`FLASH_SUPPRESS_MS=800ms` in `bridge.svelte.ts`)** — the warn toast is scheduled via `setTimeout` rather than pushed immediately; if `rig-state` arrives within the window, the timer is cancelled and no toasts surface at all. Brief rig blips (≤1s power spike, intentional power-cycle) produce zero SPA noise while genuine outages still surface within ~1.5s of detection. **Not in v1:** PTT, inbound command path, rigctld-compat TCP, NDJSON Unix-socket frontend, persistent rig-state cache (only hub caches are one slot each for `bridge-error` and `rig-disconnected` replay to late subscribers — disconnect cache clears on next `rig-state` per ADR 0009 auto-recovery).
- **`internal/ft8` subsystem** per ADR 0021 — daemon subsystem for FT8 (and related digital-mode) decode + transmit. Reverses the prior FT8-as-separate-library extraction: the integration coupling (logging, errors, config, CAT, qsoservice) is dense enough that a library depending on a specific app's internals is a misplaced subsystem, not a library. **Implementation is built from the FT8 protocol specification** (Franke/Somerville/Taylor, "The FT4 and FT8 Communication Protocols," QEX July/August 2020 — <https://wsjt.sourceforge.io/FT4_FT8_QEX.pdf> — plus companion academic refs and WSJT-X user docs), NOT translated from the WSJT-X Fortran sources — WSJT-X is GPL v3, SM is MIT, and line-by-line translation produces derivative work that would force-relicense SM. WSJT-X binaries (`jt9`, `ft8sim`) from the OS package (Fedora `wsjtx`, Debian `wsjtx`, etc.) are used only as black-box parity oracles for testing — `jt9 -8` selects FT8 mode (the `jt9` binary is WSJT-X's general decoder, historically named after the JT9 protocol; SM is FT8-only and JT9-the-protocol is out of scope). The GPL WSJT-X source distribution is off-limits as implementation reference, with one explicit carve-out: the **public-domain tarball at QEX paper reference [14]** (`ft4_ft8_protocols.tgz` from `http://physics.princeton.edu/pulsar/k1jt/`) IS reusable — it contains the LDPC matrices and short reference programs the paper authors deliberately released for reimplementers. Per QEX paper Section 9, implementations using the FT8 name must disallow robotic/unattended QSOs; SM accepts this — every QSO begins with a human-initiated TX action (M4.5 scope). CGO dependencies must be permissively licensed (BSD/MIT) — **FFTW3 is excluded** despite WSJT-X using it; KissFFT or PocketFFT take its place. Full licensing rules in ADR 0021 § Licensing constraint and `docs/v2-design/milestones.md` § Milestone 4 design preamble. **Status 2026-05-20: Layer 1 complete + Layer 2 Phase 1/2B/2C/2D + Phase 3A + Phase 3B + Phase 3C + Phase 3D shipped in `internal/ft8/codec/`** (package renamed from `internal/ft8/decoder` mid-sequence — these primitives are bidirectional). Service lifecycle scaffold from 2026-05-16 still in place; the placeholder goroutine remains until Layer 3 wires real signal-processing into Start. **Layer 1 (2026-05-17):** nine encode-side message-codec primitives, ~85 spec-vector tests pinned against the public-domain QEX ref [14] reference programs, all benchmarks zero- or one-allocation: CRC14, Pack/Unpack, CallsignC28, LDPC(174,91) matrices + LDPCEncode, Grid4ToG15, Grid6ToG25, HashCodes + HashedCallC28, FreeTextToF71, CallsignC58. **Layer 2 (2026-05-18 → 2026-05-19):** `BitBuilder` MSB-first composer (Phase 1); `Message`/`MessageType` struct + `EncodeMessage` + Type 1 packer (Phase 2B); `C28ToCallsign` + `G15ToGrid4` Layer 1 inverses (Route-B kind enums for the multi-modal c28/g15 spaces) + `DecodeMessage` + Type 1 unpacker + `stdCallToC28` routing fix (short calls 3-4 char and 5-char-2-prefix go through `HashedCallC28`, not `CallsignC28`'s negative-index byproduct) + Encode→Decode→Encode round-trip suite for long-format calls (Phase 2C); **Phase 2D (2026-05-19)** — `TokenToC28` / `C28ToToken` for the CQ-token partition per QEX Table 7 (derived analytically: `c28 = 1003 + base27(left-space-padded 4-char suffix)`; intra-row gaps fall out of the suffix-validity predicate), encoder/decoder updated to route Type 1 Call1/Call2 through tokens (token in c28 partition decodes into `Call1`/`Call2` as string), `FormatMessage` / `ParseMessage` text-layer for the canonical Type 1 patterns (CQ/CQ <suffix>/DE/QRZ/plain pair, /R rover suffix, ack-R fused with reports + separated from grids), full text→parse→encode→decode→format round-trip suite. `ErrCallsignIsToken` retired in favour of `ErrTokenInGap` for spec-violating wire patterns. Review-followup bundle SHIPPED same day: shared `validateType1Rover` helper enforces token+rover rejection symmetrically across encode + format gates (decode stays bit-faithful, asymmetry pinned by `TestDecodeMessage_TokenWithRoverIsBitFaithful`); `EncodeMessage` detaches BitBuilder storage via `slices.Clone` to future-proof against pool reuse; 3-digit-magnitude `formatSignedReport` branch pinned via corrupted-wire g15 vectors. Short callsigns surface as `ErrCallsignNeedsHashLookup` until the FT8 service layer's running 22-bit hash table lands in Phase 4. The `jt9` parity-oracle wrapper lives in `cmd/ft8-corpus-prep/` (developer tool, not runtime). **Phase 3A (2026-05-19)** — Type 0.0 ("Free Text") shipped as the first of four Phase-3 message types. New `F71ToFreeText` Layer 1 inverse (128-bit divmod via `math/bits.Div64`, trims leading spaces per `adjustr` padding asymmetry); new `Message.FreeText` field; new `encodeFreeText` / `decodeFreeText` with i3.n3=0.0 dispatch (`decodeI3Zero` sub-dispatcher reads n3 from bits[71..73] and routes — n3=0 → Free Text, n3=1/3/4/5 stay `ErrUnknownMessageType` until Phase 4); `FormatMessage` / `ParseMessage` extended with the Type 0.0 path. Parser classifier: presence of `.` or `?` (chars unique to the f71 alphabet, not in std-callsign) triggers Free Text dispatch unambiguously; inputs without a trigger char that don't parse as Type 1 still error (no implicit fallback, per Phase 3A design choice #3). Leading-space-lost asymmetry inherent to the encoding documented and pinned in tests. **Phase 3B (2026-05-20)** — Type 2 ("EU VHF /P") shipped as the second Phase-3 message type. Structural parallel to Type 1 (same wire shape — `c28 + 1bit + c28 + 1bit + 1bit + g15` — but i3=2 and the per-callsign bit slot renders as `/P` portable instead of `/R` rover, and the c28 partition is std-callsign-only with no token escape per QEX Table 7). Implemented as siblings (`encodeEUVHFP` / `decodeEUVHFP` / `formatEUVHFP` / `parseEUVHFP`) rather than parametrising the Type 1 paths — every layer has genuine per-type behaviour (token routing on encode, token rejection on decode, /R vs /P render, /P-aware parse). Per-callsign suffix-bit fields renamed `Rover1`/`Rover2` → `Suffix1`/`Suffix2` so the field name stays honest across both interpretations; `validateType1Rover` → `validateType1Suffix`; `formatStd` uses Type discriminator to pick `/R` or `/P`. New `consumePortableCall` strips `/P` and rejects `/R` as a mixed-Type input. New `validateType2Call` enforces std-callsign-only (rejects tokens). New `type2CallFromC28` rejects token-range c28 wire values as `ErrTokenInGap` (decode-side asymmetry vs Type 1, which accepts those bit-faithfully). Parser classifier extended with a `/P`-suffix pre-scan (symmetric with the `./?` Free Text trigger). **Phase 3C (2026-05-20)** — Type 4 ("NonStd Call") shipped as the third Phase-3 message type. First message type whose wire shape breaks the c28+c28+g15 mould: `h12 + c58 + h1 + r2 + c1 + i3=4` = 77 bits, with one side a 12-bit hash (`HashCodes`'s h12 width) and the other a 58-bit nonstandard callsign (`CallsignC58`). New Layer 1 inverse `C58ToCallsign` (base-38 divmod from the right; trims trailing spaces per CallsignC58's left-justified padding). New `Message.Hash12 uint16` field carries the raw 12-bit hash when the decoder can't resolve it to a string (Phase 4's running hash table will). New `wire.go` centralises Layer 2 wire-format constants (MessageBits, i3 widths/tags, n3 family, Type 4 widths + r2 token codes) — encode.go and decode.go now reference one source of truth. New `encodeNonStdCall` / `decodeNonStdCall` / `formatNonStdCall` / `parseNonStdCall`. Std-vs-nonstd shape detection picks h1; `Call1=="CQ"` sets c1; the format-side validator is laxer than encode's (accepts `<...>` sentinel + pre-bracketed strings from decoded messages). Parser classifier extended with three Type 4 triggers (angle brackets, mid-position or non-/R-non-/P trailing slash, length > 6 without slash for special-event calls like YW18FIFA); checked before /P so compound-nonstd-ending-in-/P routes to Type 4 rather than Type 2. **Phase 3D (2026-05-20)** — Type 5 ("EU VHF hashes+g25") shipped as the fourth Phase-3 message type. Contest-style exchange where BOTH callsigns hash to the wire: `h12 + h22 + R1 + r3 + s11 + g25 + i3=3` = 77 bits. New Layer 1 inverse `G25ToGrid6` (base-(18,18,10,10,24,24) divmod from the right). New `Message` fields: `Hash22 uint32` (Type 5's 22-bit second-callsign hash, paralleling Type 4's `Hash12`), `Report3 uint8` (0..7 → display 52..59 per QEX Table 2), `Serial uint16` (0..2047 s11 slot), `Grid6 string` (6-char Maidenhead, separate from the multi-modal `Grid` g15 field since g25 has no reserved-token or signed-report multiplexing). New `encodeEUVHFHash` / `decodeEUVHFHash` / `formatEUVHFHash` / `parseEUVHFHash` with `validateType5Call` / `validateType5Report` / `validateType5Serial` / `validateType5Grid` gates. Both call sides post-decode surface as `<...>` sentinels with raw `Hash12`/`Hash22` for Phase 4's table to resolve. Text layer fuses report+serial into one token (e.g. `570007` = report 5 → "57" + serial 7 → "0007"); brackets BOTH calls in display (renderType5Call mirrors renderType4Call's three-shape acceptance). New `isType5Trigger` classifier: BOTH first two tokens are angle-bracketed AND last token is a 6-char Maidenhead grid → Type 5; the Free Text classifier was tightened to require absence of angle brackets (so the `<...>` sentinel doesn't trip `.` → Free Text routing). Dispatch order: Free Text → Type 5 → Type 4 → Type 2 → Type 1. **Phase 4 hash table (2026-05-20)** — receiver-side running callsign-hash table per QEX §6 SHIPPED in `internal/ft8/codec/hashtable.go`. Bounded capacity (default 100, WSJT-X convention) with FIFO eviction of the oldest entry + LRU-on-reinsert (re-Inserting a present callsign moves it to MRU). Thread-safe via `sync.RWMutex`. `Insert(callsign)` trims whitespace, silently filters non-callsigns (empty, `<...>` sentinel, tokens via `TokenToC28`, out-of-alphabet, > 11 chars), and stores precomputed h10/h12/h22. `LookupH22` / `LookupH12` / `LookupH10` scan newest-to-oldest so the most recently Inserted call wins on hash collision; collision rates at cap=100 are h22≈0.002% / h12≈2.4% / h10≈10%. `Observe(Message)` walks Call1/Call2 and Inserts each (per-Type dispatch unnecessary — Insert's filter handles tokens/sentinels). `Resolve(Message) Message` returns a copy with sentinel call slots replaced: Type 4 uses `Hash12` → whichever of Call1/Call2 is the sentinel; Type 5 uses `Hash12` → Call1 AND `Hash22` → Call2; resolved Hash fields are zeroed; other Types return unchanged. **Session 77 spec-correctness sweep (2026-05-21) shipped six commits:** (i) **i3=5 mis-assignment fix** — Phase 3D had shipped Type 5 (EU VHF hashes+g25) at wire i3=3 instead of the QEX-Table-1-mandated i3=5; constants renumbered + test pins updated, i3=3 freed for Type 3. (ii) **Finding #1** — `CallsignC28` rewritten with digit-position-3 alignment so short calls (3-char, 4-char, 5-char-2-prefix shapes like K1JT/G3X/AB1CD/VK7MO) pack into the `[stdCallOffset, 2^28)` std-call range per QEX Appendix A Table 7's "Standard call signs" row ("28 bits are enough to encode any standard call sign uniquely"); the `stdCallToC28` / `isLongFormatStdCallsign` / `HashedCallC28`-routing workaround dropped; spec-vectors regenerated (M1A 6,050,834 → 10,608,568; K1JT 6,040,944 → 10,222,009; VK7MO 12,339,580 negative-index byproduct → 237,090,319 spec-correct); new property test runs 5,000 random std-shape calls and pins all land in the std-call range. (iii) **Finding #2** — Type 2 c28 now accepts tokens per QEX Table 2 (c28 = "Standard callsign, CQ, DE, QRZ, or 22-bit hash" universally); `validateType2Call` delegates to `validateType1Call`; new `validateType2Suffix` mirrors `validateType1Suffix` (rejects /P on a token); `parseEUVHFP` refactored into dispatcher + `parseCQEUVHFP` / `parseDirectedEUVHFP` / `parsePlainEUVHFP` mirroring `parseStd`'s shape; "CQ G4ABC/P JO22" round-trips. (iv) **Finding #3** — `ParseMessage` now falls back to Type 0.0 Free Text when structured parse fails AND `validateFreeText(trimmed)` accepts (≤ 13 chars in f71 alphabet), so QEX Table 1's canonical example "TNX BOB 73 GL" (no `.`/`?` trigger) parses correctly; eager `.`/`?` dispatch unchanged. (v) **Finding #4** — `G25ToGrid6` signature changed from `(uint32) string` to `(uint32) (string, bool)`; out-of-range g25 (≥ maxGrid6, ~44% of 25-bit codepoints) returns `("", false)` instead of panicking; `decodeEUVHFHash` wraps as new `ErrInvalidGrid6` sentinel. (vi) **Type 3 RTTY Roundup SHIPPED end-to-end** — new `internal/ft8/codec/rttyroundup.go` Layer 1 primitive for the multi-modal s13 exchange slot (serial 0..7999 OR state/province from the 65-entry QEX ref [14] `states_provinces.txt` table; `S13Kind` enum + `SerialToS13` / `StateToS13` / `S13ToExchange` API), new `Message.TU` + `Message.StateProvince` fields, new `encodeRTTYRoundup` / `decodeRTTYRoundup` / `formatRTTYRoundup` / `parseRTTYRoundup` (full TU + R-ack + token-aware Call1 + serial-or-state exchange support), new `ErrInvalidS13` for unassigned s13 codepoints (8000, > 8065 — same panic-to-error pattern as finding #4), classifier trigger `isType3Trigger` fires on `"TU;"` first-token OR any `"5N9"` 3-digit report token (N ∈ 2..9). Type 1 short-call decode still errors `ErrCallsignNeedsHashLookup` for c28 values in the hash partition — but now that partition only holds legitimate non-std-call hashes (Type 4 → c58 → later h22 reference); surfacing those as `<...>` + `Hash22` matching Type 4/5's pattern is the deferred follow-up. **Remaining Phase 4 scope:** Type 0.1 (DXpedition, `c28 c28 h10 r5`), Type 0.3 (Field Day ARRL, `c28 c28 R1 n4 k3 S7`), Type 0.4 (Field Day RAC, same shape as 0.3), Type 0.5 (Telemetry, `t71`). After Phase 4 closes, Layer 3 (LDPC decoder + signal-processing pipeline) opens. Same in-process-default + opt-in-split-host (`cmd/ft8d`) shape as the bridge per ADR 0013. M4.1 progress table lives in `docs/v2-design/milestones.md` § Milestone 4.1.
- **CD pipeline (Continuous Delivery) shipped 2026-05-16** at `.github/workflows/ci.yml` — gates every push to main on SPA lint/check/test/build, gofmt drift, go vet, `go test -race`, daemon embed-build, all `cmd/...` builds. Trunk-based workflow (no feature branches); the gate IS the merge protection. Local mirror via `task ci:local` runs the same steps. Releases stay operator-driven (`git tag` + `scripts/release-rpm.sh`); the pipeline only answers "if I tagged this commit right now, would the release artifact be sound?"
- **Local-dogfood update task `task deploy:local:dev`** — one command: build dev RPM → stop user-level `smd` → `sudo rpm -Uvh --replacepkgs` → systemd daemon-reload → start → status check. Use after a code change to refresh the dogfooded install without ceremony. Sudo prompts once per session for the package install; systemctl commands run as the user.
- **`v1` branch** (and the `v1.0.0` tag) preserves the last v1 release (a Wails app with no daemon process). Day-to-day ham radio operations run from this branch; v1 bug fixes land here.
- **`pre-ft8-removal` tag** preserves the FT8 experiment tree in history for recovery if needed (superseded by ADR 0021 which restarts FT8 inside SM; the tag remains useful as a reference for the v1-era Fortran integration).

**`/v1/` in the daemon's URL prefix is API versioning (the API's first iteration), unrelated to the project's v1/v2 distinction.**

## Where the durable project context lives

Read these before making non-trivial design choices. They are the reference, not the scratchpad.

- **`docs/v1-analysis/invariants.md`** — load-bearing rules that must carry forward into v2. Check proposals against this list.
- **`docs/v1-analysis/lessons-for-v2.md`** — synthesis: patterns to apply, patterns to avoid, what v1 got right. The single most important read before any v2 design choice.
- **`docs/v1-analysis/design-decisions-log.md`** — keep/change/delete verdicts on every v1 major shape decision, plus the v2 rewrite decision.
- **`docs/v1-analysis/bug-inventory.md`** — known v1 issues, fixed and open. The "do not recreate these" list for v2.
- **`docs/v1-analysis/architecture-map.md`** — what v1 actually contains, module by module.
- **`docs/v2-design/`** — v2 design decisions as they're made. `structure.md` is the first; siblings (`api.md`, `milestones.md`, `db-layer.md`, etc.) appear as the corresponding questions get answered.
- **`docs/decisions/`** — append-only ADR log. One file per decision, numbered, with `status` field. Captures the reasoning trail (alternatives considered, why each lost, what would change the answer) for decisions that get revisited. Format and lifecycle in `docs/decisions/README.md`; copy `template.md` to start a new one. Use this when a choice has plausible alternatives that were genuinely weighed; skip it for routine code-level choices or one-obvious-answer decisions.
- **`docs/session-handoff.md`** — rolling cross-session state. Read at session start; update at session end.
- **`docs/keyboard-shortcuts.md`** — running inventory of every keyboard shortcut wired up in the SPA. Source-of-truth for the user-facing manual; update in the same commit as any code change that adds, removes, or rebinds a shortcut.
- **`docs/install.md`** — operator-facing install and first-run setup guide. Update when packaging, the unit file, the data-dir path, or the first-run flow changes; this is the doc a new user is expected to read end-to-end.

## Load-bearing invariants (headlines)

Full list in `docs/v1-analysis/invariants.md`. Never violate these without explicit discussion.

- **Enrichment never blocks logging.** External lookups (hamnut, QRZ) degrade the QSO draft's completeness when they fail; they never prevent the operator from logging. The only thing that should stop logging is a broken local DB.
- **One-fails-all-fail for QSO writes.** The QSO row and its upload-queue row(s) are atomic in a single transaction; cache/enrichment writes live outside the transaction as best-effort.
- **Narrow daemon scope (package-boundary, per ADR 0013 + ADR 0021).** The daemon's log/forward subsystems are narrow — ingest, validate, store, forward, emit status, serve queries. Rig control / CAT / PTT / audio / FT8 decoding / capture UX must not couple with those subsystems. Under ADR 0013 the bridge runs as a daemon subsystem (`internal/bridge`) in the default deployment; under ADR 0021 FT8 also runs as a daemon subsystem (`internal/ft8`) in the default deployment. Both rules are enforced at the **package-import graph**: `internal/storage` and `internal/forwarder` must not import `internal/bridge` or `internal/ft8`, and vice versa. `internal/bridge` and `internal/ft8` are siblings — they MUST NOT import each other (both consume `internal/cat`/`internal/serial`; if FT8 ever needs rig state from the bridge, the route is a neutral package, not a direct dependency). FT8 is allowed to import `internal/qsoservice` (it's a CONSUMER — submits decoded QSOs via `qsoservice.Submit`); bridge is not. The split-host opt-ins (separately-built `cmd/bridge`, future `cmd/ft8d`) preserve the original process-boundary enforcement for those topologies. Either way, a proposal that wires log/forward code through the bridge or ft8 packages (or vice versa) contradicts a decided constraint. Boundary tests in `internal/bridge/boundary_test.go` and `internal/ft8/boundary_test.go` defend both directions; CI catches violations.
- **`types.Qso` follows the ADIF specification.** The data model mirrors ADIF; adding a new ADIF field is a one-line change. The shape is load-bearing — do not propose flattening or reshaping without checking the invariants doc first.

## Lessons to apply (headlines)

Full list in `docs/v1-analysis/lessons-for-v2.md`. These patterns were explicitly extracted from v1's mistakes; each one has a cautionary tale attached.

- **Build specific, not generic.** Three specific implementations are easier to read, debug, and evolve than one clever abstraction. A small amount of duplication is cheaper than a framework. Don't DRY things up prematurely. Cautionary tale: v1 `internal/adapters/` — 30+ test files of reflection-based adapter framework, abandoned as "too complicated to maintain and use correctly."
- **Integration tests over mocks.** Real `&sqlite.Service{}` with in-memory databases is the pattern v1 uses correctly. If an interface's only consumer is a mock, delete the interface and the mock. Cautionary tale: v1 `DatabaseServiceInterface` drifted out of sync with the concrete type because nothing real ever used it.
- **Document intent, not mechanism.** Code shows what; doc comments should explain why. Non-obvious design decisions deserve a `doc.go`. Cautionary tale: the `additional_data` blob pattern had no `doc.go`, so the ADIF-alignment intent had to be rediscovered during analysis.
- **Asymmetric round-trips are a clue.** If a converter pair has one elegant side and one manual/complicated side, the complicated side is overbuilt. Cautionary tale: the `QsoAdditionalData` intermediate struct had a 100-line hand-mapped forward direction next to a 20-line `json.Unmarshal` reverse direction; the asymmetry was the code telling us the forward direction was wrong.
- **Explicit fallbacks for every external dependency.** The first test to write for enrichment code is "what happens when the service is down," not the second. Cautionary tale: hamnut-blocks-logging — a failed hamnut lookup propagated as fatal and prevented QSO entry entirely.
- **Transaction boundaries are conscious design.** Multi-table writes need an explicit decision about what's atomic and what's best-effort. Cautionary tale: v1 `LogQso` ran four sequential non-transactional writes and could leave a QSO stored without its upload-queue row.
- **Characterization tests before refactoring.** Write tests that freeze current observable behavior first; then refactor against them. Don't trust "CI passes" as evidence the refactor is correct unless CI was exercising the behavior you touched.
- **Enumerate all API consumers before designing any endpoints.** The three client apps (logging, logbook, config — now Svelte SPAs per ADR 0001, originally planned as Wails apps) have different needs; a logging-centric API sketch will miss the logbook-management surface entirely.

## Code style

Standard Go idioms (`gofmt`, `goimports`, Effective Go). Project-specific notes that override generic advice:

- **Short single-letter names are encouraged** for tight scopes and method receivers — consistent with Go convention (`r Reader`, `s *Service`, `t *T`). Avoid generic names like `data`, `item`, or `value` unless the context makes purpose obvious.
- **Use `internal/errors` for operation-tagged errors.** The canonical idiom is a package-scoped `const op errors.Op = "pkg.Func"` followed by `errors.New(op).WithErr(err).WithMsg("context")` (or `.WithMsgf(...)` for formatted messages). All builder methods are nil-safe and chainable. Prefer this over `fmt.Errorf` in code that belongs to the tagged-error paths. There is no `Errorf` convenience method — if you need to wrap a formatted cause, use `.WithErr(fmt.Errorf("...: %w", err))` explicitly. See `internal/errors/doc.go` for the full pattern and `docs/reviews/internal-errors.md` for the review that shaped it.
- **Use `internal/iocdi` for service lifecycle and dependency wiring.** The `di.inject:"..."` struct tag convention on Service fields is how dependencies are resolved. Don't hand-wire services in new code; register them with the container.
- **Comments explain *why*, not *what*.** Well-named code documents its own mechanism. Document non-obvious constraints, invariants the code relies on, and the reason a specific approach was chosen over alternatives. Don't narrate what a function obviously does; don't reference specific callers or tickets (that belongs in PR descriptions).
- **Keep functions short and focused** on a single responsibility. Avoid deep nesting and long parameter lists.
- **Line length 100–120 characters** is a reasonable ceiling; `gofmt` doesn't enforce it but readability suffers past that.
- **For Svelte frontends:** prefer Svelte 5 runes (`$state`, `$derived`, `$props`) over Svelte 4 syntax. Use snippets for reusable UI chunks within components when logic is tightly coupled; reach for separate files when it isn't.
- **SPA lint + format are wired up.** From `frontend/logging/`: `npm run lint` (eslint with type-checked TS rules), `npm run lint:fix`, `npm run format` (prettier across `src/`), `npm run format:check`. ESLint config is the modern flat format (`eslint.config.js`); prettier owns formatting via `eslint-config-prettier`. When a lint disable is genuinely warranted (e.g. svelte-eslint-parser inference gaps, intentionally non-reactive plumbing inside a `.svelte.ts` module, two-way `bind:value` patterns that look like passive `$state` mirrors but aren't), write a why-comment immediately above the `eslint-disable-next-line`. Never a bare disable.

## Project idioms

Conventions that carry forward from v1 and that new v2 code should follow. These are real project rules, not general Go advice.

- **`internal/types` only imports the Go standard library.** It is deliberately dependency-free to prevent cyclic dependencies — the whole project imports from `types`, so anything `types` imports is transitively imported everywhere. Adding a non-stdlib import to `types` is almost certainly a mistake; push the dependency outward into the consumer package instead.
- **Reuse `types.X` rather than building parallel structs.** When a config block, request body, or response payload describes the same shape as an existing `types.X` struct (e.g. `types.Qso`, `types.LoggingStation`), embed or alias the canonical type — don't define a parallel `XConfig`, `XRequest`, or `XPatch` that duplicates fields. Parallel structs drift, double the maintenance cost, and bury the ADIF/JSON tag conventions that already live on `types.X`. Use `json.Unmarshal` overlay + stash-restore for read-only fields when only a subset is settable; build a parallel struct only when fields fundamentally don't overlap.
- **Service lifecycle pattern.** Services follow `Initialize()` → `Open()` / `Start(ctx)` → `Close()` / `Stop()`, with all lifecycle methods idempotent. `Initialize()` validates config and dependencies; `Start` / `Open` begins operation; `Stop` / `Close` does graceful shutdown. New services should match this shape for composability with existing DI wiring.
- **DI `ServiceName` constants.** Each service exposes a `ServiceName` constant used as its bean ID in the `iocdi` container. Shared service names live in `internal/types/services.go` (e.g. `types.SqliteServiceName`); app-specific services define their own locally (e.g. `facade.ServiceName = "logging-app-facade"`). This convention keeps DI wiring discoverable by grep.
- **sqlboiler-generated models are not hand-edited.** Files under `internal/database/sqlite/models/` are regenerated from the schema; hand edits get clobbered on the next run. The ORM choice itself is undecided for v2 (see `docs/v1-analysis/design-decisions-log.md` → "ORM / query generator choice"), but while sqlboiler is still in use, treat generated output as read-only.
- **Runtime data path resolution via `utils.WorkingDir()`.** Anything that needs the on-disk data directory (SQLite DB, logs, config) calls this helper. Resolution order: explicit argument → `SM_WORKING_DIR` env var → the executable's own directory. Don't hand-roll path resolution elsewhere.

## Error handling

- **Handle errors explicitly.** No silent failures. Log errors with operation context using the `errors.Op` pattern.
- **Enrichment errors are never fatal.** External service failures log a warning and fall through to a cached value or a default. See the "Enrichment never blocks logging" invariant.
- **Wrap errors with meaningful context** as they propagate; don't return naked errors from deep call stacks.

## Testing

- **Integration tests are the default** for anything touching storage, services, or cross-package flows. Use real `&sqlite.Service{}` with in-memory SQLite databases. Do not introduce mock interfaces for internal services.
- **Unit tests for pure logic** — parsing, math, format conversions, adapter round-trips. No mocks needed.
- **Test the error path first for enrichment code.** Per the invariant: "what does this do when hamnut/QRZ is down" is the test that should exist before the happy-path test is written.
- **Characterization tests before refactoring** untested code. Freeze the current behavior with tests, then refactor.
- **Cover edge cases and error paths,** not just the happy path.
- **Go:** standard `testing` package. `github.com/stretchr/testify` is fine when it makes assertions cleaner.
- **TypeScript/Svelte:** Vitest for unit and component tests; Playwright for E2E.

## Dependencies

- **Minimize external dependencies.** Prefer the standard library. Document why a non-obvious dependency is needed.
- **Home-grown choices are deliberate.** `internal/iocdi` (DI), `internal/serial` and `internal/cat` (rig control), the custom ADIF parser — all chosen after considering alternatives and judged lighter or better-fit than the popular options. Don't suggest replacing them with `uber-go/fx`, `google/wire`, hamlib, etc. without a concrete reason specific to this project's needs.
- **Keep dependencies current** but don't upgrade prophylactically during unrelated work — version bumps get their own commit.

## Commits and version control

- **Atomic commits with meaningful messages.** One logical change per commit. Commit message explains the *why*, not just the *what*.
- **Don't commit secrets.** `.env`, credentials, configuration with real tokens are gitignored; don't add them.
- **Don't touch the `v1` branch** unless fixing a v1 bug the user surfaced while running the software. Main is where v2 work happens; v1 is frozen except for user-reported fixes.
