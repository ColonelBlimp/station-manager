---
title: Chapters
# These pages exist only to be assembled into the single-page manual by
# layouts/index.html. render:never suppresses standalone per-chapter output
# (and the otherwise-needed page/section templates); list defaults to "always"
# so they remain in .Site.RegularPages for the home template to range over.
build:
  render: never
  list: never
cascade:
  build:
    render: never
---
