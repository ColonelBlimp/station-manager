---
title: "Appendix: Command-Line Reference"
weight: 220
appendix: true
---

*Draft — the `smd` / `smctl` subcommands an operator runs (e.g. `smctl import`, including the `--forward` flag for opting imported QSOs into a forwarder upload; default is no upload).*
