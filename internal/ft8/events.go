package ft8

// ServiceName is the DI bean ID for the FT8 service. Used by the
// iocdi container and by other services that need to fetch the FT8
// subsystem by name. Mirrors the pattern in internal/bridge.
const ServiceName = "ft8service"
