package main

func main() {
	// TODO(v2 milestone 1): wire up iocdi container, open sqlite, start HTTP
	// server on a Unix domain socket, register qsoservice + api handlers.
	// See docs/v2-design/structure.md and (once written) docs/v2-design/api.md.
}
